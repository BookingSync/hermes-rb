require "spec_helper"

RSpec.describe Hermes::RpcClient do
  # there is already an integration spec for it
  # due to the overall complexity with condition variables, and tight coupling to Bunny interface
  # adding a unit spec might just not be worth it
end
